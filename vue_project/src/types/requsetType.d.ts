export interface login {
    email: string,
    password: string
}

export interface register {
    email: string;
    name:string,
    password: string;
    password2:string,
    identity:string,
}